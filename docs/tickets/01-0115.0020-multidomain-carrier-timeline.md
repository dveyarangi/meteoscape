# Multi-domain carrier and the timeline rework

- **Status:** Ready
- **Type:** AFK
- **RFC:** [0012](../rfc/0012-20260808-multidomain-carrier-timeline.md) — closes the fold/carrier
  naming checkpoint (`agreed_geometries` / `CoverageGroup`)
- **Outcome:** The Provider answers its natural shape — multi-domain on open axes, natural fetch
  unit on parameters. Behavior unchanged for all live callers (no in-tree author of `ANY` yet).

## Parent

[Retentive store](./01-0115-retentive-store-freshness.md) — slice 2 of 4. The parent carries the
full decision record; this ticket carries only its slice's scope and criteria.

## What to build

~250–350 raw lines (002b-scale):

- Mint the **multi-domain carrier**: a `Manifold` grouping single-domain Coverages; its own
  `project` folds onto a fully enumerable Selection (the retirement home of `_assemble` — closure
  preserved) → parent, *The carrier, and the fold that returns it*.
- `agreed_geometry` → **always returns a group**: bounded axes must agree (permanent law),
  boundless members license per-record difference, a fully-bounded request's group is a degenerate
  single unwrapped at both call sites. The fold's and the carrier's final names are chosen here
  (the edge record's naming checkpoint).
- Timeline wrapper rework: **retire both eager folds** — the Z flatten (`_assemble` at fetch) and
  the parameter crop (`_as_delivered`) — under the parent's one law: *the answer carries the
  provider's natural shape; the store absorbs it; the serve crops.*
- **Natural fetch unit**: the answer may carry more parameters than asked, never fewer;
  Open-Meteo's is its whole offering → parent, *Refill scope*;
  [#43](../concerns.md#43-narrow-answering-providers-re-open-mixed-request-run-divergence).
  ADR-0001's answer-discipline sentence lands with this code.

## Acceptance criteria

- [ ] Asked with boundless Z/T (direct provider test), the timeline answers **multi-domain** —
      records keyed by native Z (2 m, 10 m, surface, `[0,TOA]`) from a **single** vendor fetch.
- [ ] Asked for a parameter subset, the answer carries the whole offering (natural fetch unit); an
      enumerable ask still yields one co-domained Coverage (closure).
- [ ] Projecting the carrier with a fully enumerable Selection reproduces today's folded answer.
- [ ] Every live path is behavior-identical: the full existing suite passes unchanged; the eager
      folds' retirement is observable only to direct callers asking `ANY`.
- [ ] The edge record's naming checkpoint is closed — the chosen fold and carrier names are
      recorded there and the singular-geometry name is gone from the code.

## Blocked by

- [`ANY` as the boundless snapped member](./done/01-0115.0010-any-boundless-member.md) — complete.

## Parent scope addressed

Parent acceptance criteria 2 (Provider answers multi-domain from a single fetch) and the
no-eager-flatten half of 4; enabling work for 7 (natural fetch unit). The Z-reuse half of 4 needs
the store and lands at slice 4.
