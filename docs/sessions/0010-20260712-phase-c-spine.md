# 0010 · 2026-07-12 · Phase C spine landed — issue 001 closed

Continues [session 0009](./0009-20260712-phase-c-spine-plan.md). Implemented the walking-skeleton
**Phase C** per the plan's TDD cycle list — no deviations from the planned decisions. All five
acceptance criteria of [issue 001](../tickets/done/001-walking-skeleton.md) are met; the
full gate is green (71 tests, ruff, pyright).

## Done

- **Open-Meteo leaf** (`nodes/providers/open_meteo.py`): provider + minimal scalar normalizer
  (`temperature_2m` → `air_temperature`, unit *verified* not converted), cadence constants,
  Provider-authored `Uniform` provenance, `MANIFEST` registered in `server.py`.
- **Transport seam** (`nodes/providers/base.py`): `Transport` / `FetchRequest` / `HttpxTransport`
  (decoded JSON out; status / timeout / non-JSON → `RuntimeFailure`; connect `retries=2`).
- **Composite behaviour**: `Reservoir.project` pure delegate; `Arbiter.project` real admission —
  per-parameter omit, whole-request `CapabilityMismatch` only when nothing is producible, one
  projection via the newly minted `Selection.with_params`, different-winners
  `NotImplementedError` guard (per-parameter assembly → 005).
- **MCP edge** (`api/mcp_app.py`): `forecast_hourly` — floor-now 168 h Selection with exact 2 m
  Z cell, lat/lon + unknown-parameter + supplied-window `bad-request`s, compact serialization
  (`{unit, values, provenance: {source, exp}}`, nodata → `null`), taxonomy → `ToolError` stable
  prefixes, capability-narrated tool description.
- **Wiring regression**: `RootStoreSpec` → `StoreSpec` everywhere (`OfferingSpec.default_lattice` /
  `source_lattice` dropped, `StoreFactory.create(EnumerableDomain | StoreSpec | None)`, StubStore
  dummy `domain`); `open_meteo_enabled=True` default.
- **The e2e** (`tests/test_e2e_forecast.py`): respx-mocked transport, in-memory FastMCP client,
  full JSON response asserted — plus the second-call **re-fetch** assertion documenting
  no-retention, for 006 to flip.

## Open / continuation

- **002 — core 5 parameters**: `Channel`/`VendorVar` N:M table (forced by the wind vector case),
  extensive `serves` horizon edge, capability grows off `OfferingSpec.parameters`.
- **Concern #24** (mixed-height bundle: fat shared Z cell vs per-Z-group edge Selections) —
  decision point at 002/003 with the fetch-cost trade in view.
- **003** — `parameters`/`start`/`end` implementation + edge alias table (the `bad-request:
  not yet supported` stubs come alive).
- **004 — second provider + fallback**: `RuntimeFailure` fall-through gets its test; the
  cross-provider thesis meets reality.
- **006** — retentive `Store` replaces `StubStore`; flips the e2e re-fetch assertion; consumes
  the `StoreSpec` knobs / provider-exact domains that Phase C only threads through.
