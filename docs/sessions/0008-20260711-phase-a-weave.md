# 0008 · 2026-07-11 · Phase A weave landed

Continues [session 0007](./0007-20260711-phase-a-weave-plan.md). Implemented the walking-skeleton
**Phase A** build seam from [issue 001](../../issues/20260623_v1/001-walking-skeleton.md); refined
Weaver/Arbiter and store-factory shape while green.

## Done

- Binders (`SourceBinder` / `CalculatorBinder` + `CompositionError`), `Weaver`, `compose` → `Gateway`,
  empty production catalogues, config defaults (`open_meteo_enabled=False`, empty calculators).
- Tests mirror `src` (`tests/nodes/…`, `tests/api/…`, `tests/test_server.py`); shared `tests/fakes.py`.
- **Arbiter** owns reconciler policy: `Arbiter(sources, SourceRegistry, ArbiterPolicy)` — ranks from
  registry priority; Weaver only builds the Source map + allocates stores.
- **`StoreFactory`** is a concrete class (`create` → **`StubStore`**); no factory protocol /
  `PassThroughStore` naming.

Contract docs synced: architecture / ADR-0004 / ADR-0005 / glossary.

## Open / continuation

- **Phase B** — value-type behaviour (`RegularAxis` / `RegularDomain` / `FootprintDomain.contains` /
  `Timeline.project`, …) per issue 001.
- **Phase C** — Open-Meteo leaf, catalogue registration, `open_meteo_enabled=True`, MCP path.
- **002b** — Calculator graph weave; wind specs return to `Settings`.
- **006** — retentive `Store` replaces `StubStore`; store-grid representation decision.
