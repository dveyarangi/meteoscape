# 0021 · 2026-08-05 · 003c re-staged on m4; 006 pulled ahead; five review passes

**Scope:** /recall of the post-m4 state; an /align that re-staged
[RFC 0008](../rfc/done/0008-20260725-003c-request-shaping.md) against the landed Snapped-T mode —
three decisions, a queue reorder, and one newly minted 006 decision; then five /plan-impl review
passes over the re-staged RFC, each under a progressively strengthened rule set (the skill gained
its architecture-leads, teaching-pass, and unambiguity paragraphs between passes).

## Work done

- **Recall**: m4 landed and closed; 003c and 011 both Ready. Chose 003c; its recorded first step
  was the RFC 0008 re-stage, run as an /align.
- **Align, three questions**: (1) re-stage **in place**, clamp machinery compressed into the RFC's
  §Superseded approach; (2) the [#42](../concerns.md#42-two-request-representations-so-resolution-cannot-be-a-method)
  representation call — **the split stays**; (3) the two-fetch divergence judgement — **accepted
  rare-and-loud**, sharpened by the user's challenge into its honest form: retention collapses the
  **warm path only**, because neither the engaged-tap fetch nor 006's drafted per-parameter refill
  widens across parameters, so a cold store still double-fetches disjoint variable sets. That
  sharpening minted **refill scope** as a 006-align decision and drove the queue reorder:
  **006 → position 0115, directly after 003c** ("cache is the mechanism, not workarounds").
- **Doc landing**: RFC 0008 rewritten in place; resolutions struck into the
  [003c ticket](../tickets/done/01-0110-request-shaping.md)'s criteria; ticket file renamed
  `01-0130` → [`01-0115`](../tickets/01-0115-retentive-store-freshness.md) with ~30 inbound links
  swept; [#30](../concerns.md#30-response-membership-under-runtime-degraded-fallback) and #42
  updated; [delivery status](../tickets/README.md) reordered, 006 flipped Ready, and its stale
  "store-lattice representation" line (resolved at m4) replaced with refill scope.
- **Five review passes** over the RFC, converging: (1) correctness — the `Metronome`-floors-to-the-
  hour fact corrected the implicit-start consequence, the stage-1 test inventory and rolling-reach
  fixture rule restored, error messages pinned; (2) source facts — the `build_mcp_app` call-site
  count corrected to eight (positional-arg sites invisible to a kwarg grep), smoke test identified
  as the empty-menu pin; (3) architecture sanction — every flow traced to architecture.md's own
  sentences, the fetch-count/no-retention assumption confined to its one commented pin; (4)
  teachability — the "why not a sentinel end" question exposed and answered in decision 7 (the
  `min` fold is what keeps a diverging-reach response one shape — invisible in v1, load-bearing at
  the second provider); (5) unambiguity — test names, explicit e2e bounds, the `_format_horizon`
  formula, and stage-4 scenarios pinned so the RFC states a single implementation path.

## Settled this session

- **RFC 0008 is re-staged and implementation-ready**; the clamp record survives in its
  §Superseded approach → [rfc/0008](../rfc/done/0008-20260725-003c-request-shaping.md).
- **The request side keeps two representations at 003c** — the edge authors `SelectionDomain`,
  `ground` stays a function; narrowing waits for the second author →
  [003c ticket](../tickets/done/01-0110-request-shaping.md) criterion,
  [#42](../concerns.md#42-two-request-representations-so-resolution-cannot-be-a-method).
- **Two-fetch divergence: accepted rare-and-loud for exactly one ticket**, no mitigation; the
  landing re-confirms → [003c ticket](../tickets/done/01-0110-request-shaping.md) criterion,
  [#30](../concerns.md#30-response-membership-under-runtime-degraded-fallback).
- **Retention dissolves the common case only** — the cold-store disjoint-parameter double fetch
  survives 006 as drafted → [#30](../concerns.md#30-response-membership-under-runtime-degraded-fallback),
  [retentive-store ticket](../tickets/01-0115-retentive-store-freshness.md).
- **The retentive store precedes Visual Crossing** (position 0115, Ready) →
  [delivery status](../tickets/README.md).
- **Refill scope** (requested-parameters vs whole-offering) **and the partial-warm
  window-extension verification are 006-align decisions** →
  [retentive-store ticket](../tickets/01-0115-retentive-store-freshness.md), delivery status
  "Decisions still owned by tickets".

## Open questions

All owned elsewhere; none live only here:

- **Refill scope** — decides whether the cold-path divergence residue also dies →
  [retentive-store ticket](../tickets/01-0115-retentive-store-freshness.md).
- **Whether 006's refill narrows the request side to one representation** →
  [#42](../concerns.md#42-two-request-representations-so-resolution-cannot-be-a-method), the
  ticket's trigger paragraph.
- **The divergence judgement's landing re-confirmation** and the mixed-reach fold policy →
  [003c ticket](../tickets/done/01-0110-request-shaping.md) criterion,
  [#30](../concerns.md#30-response-membership-under-runtime-degraded-fallback).
- **Parity-existence enforcement** remains the provider edge's unguarded promise →
  [#41](../concerns.md#41-parity-evidence-is-unenforced-and-unrouted).

## Continuation

- **Implement 003c from the re-staged RFC** (stages 1–5; surface adapter only). The landing runs
  live parity as the 384-tick acceptance probe, updates the
  [MCP edge record](../edge/mcp.md)'s Contract (a named-compatible change), and re-confirms the
  divergence judgement against shipped behaviour.
- **Then 006**: its align opens with refill scope, the partial-warm verification, and #42's
  narrowing question — with `ground` read first, per the ticket, so `quantize` stays its sibling.
- The untracked `.agents/skills/wayfinder/` skill predates this session's scope and stays
  uncommitted here.
