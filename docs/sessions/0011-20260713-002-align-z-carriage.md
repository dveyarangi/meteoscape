# 0011 · 2026-07-13 · 002 align — Z carriage: vantage/exact modes, axis-kind matching

Continues [session 0010](./0010-20260712-phase-c-spine.md). Align session on
[issue 002](../../issues/20260623_v1/002-core-5-parameters.md); the first question — the
[concern #24] mixed-height bundle — consumed the session and **resolved it**, closing a design loop
that also positioned concern #13's Z half and rewrote the Phase C fat-footprint reading.
Remaining 002 align questions (internal-only carriage, `Channel` arity, vendor units, precipitation
variable) are deferred to the next session.

## The decision

**Z carriage rides the existing mode-in-shape rule (ADR-0002), applied per axis; matching is
axis-kind-owned; declarations are facts.** In full:

- **Declarations are native facts, never widened**: temperature = sample @ `2 m`; wind = samples at
  levels; cloud cover = the served cells `{[0,2 km], [2,6 km], [6,TOA], [0,TOA]}`; Open-Meteo X/Y =
  global (a real vendor commitment — server-side interpolation). Phase C's `_NEAR_SURFACE_Z = [0,10]`
  fat capability footprint is **interpretation polluting the fact slot** and is replaced at 002.
- **Request Z mode = the Domain shape on Z**: **Continuous extent = vantage mode** ("I'm somewhere in
  this window") — the default bundle's edge-authored acceptance window (`[0, ~10 m]`); **exact cell =
  sampling / cell-addressing mode** — 003's aliases (`temperature_2m`, `cloud_cover_low`,
  `soil_temperature_6cm`) desugar here.
- **Matching is axis-kind-owned** (uniform `declared.matches(requested)` dispatch; precedent:
  `RollingAxis` clock-window matching; `extent_scaling`-branched `serves`). The **quantifier rule**
  grounds the direction: a *sample* is an ∃-claim → membership (`2 ∈ [0,10]`); a *cell statistic* is a
  ∀-claim over its scope → inclusion (`[0,10] ⊆ [0,TOA]`). One request legally evaluates both
  directions because each declaration type owns its own arithmetic.
- **Maximal-served-cell rule**: a vantage request resolves a cell-statistic parameter to the served
  cell containing all other served cells (total, for cloud layers); none exists → honest omission.
  Exact-cell requests match their cell precisely (never overridden). Vantage-resolvability is
  **emergent from capability** — no `requestable-in-vantage` flag.
- **Resampler/Calculator boundary**: *resampler = same value, new geometry, no assumptions* (entailed
  by scale × statistic × extent_scaling); *Calculator = new value, declared assumptions, synthetic
  provenance*. Multi-level samples in a window → resampler representative (linear via u/v). Cloud
  layers → total needs an overlap assumption → **Calculator** (`cloud_cover @ [0,TOA]`, lineage
  low/mid/high) — the same gap-filling move as between-levels wind interpolation.
- **Closed projection intact**: response rides `sel.domain` (Z cell = the window, offsets absorbed
  into `bounds` per ADR-0002); native cells stay in the native record, recoverable via `SourceKey`.
  `Selection` stays one-domain; the session 0009 decline of per-parameter domains stands.
- **Vertical references**: confirmed already-decided (ADR-0002 — one `vertical_reference` per Z axis
  per Domain ⇒ single-reference requests by construction; cross-frame = Calculator). A proposed
  `ReferenceAxis` was withdrawn as solving a solved problem.

## Rejection trail (the structural loads)

1. **Fat union request** — the caller's domain conflated with native geometry; a column parameter
   inflates the union and breaks containment for narrow parameters.
2. **Fat capability reach + point request ("B2")** — consumer interpretation in the fact slot
   (indicts shipped `_NEAR_SURFACE_Z`).
3. **Precise capabilities + Z-intersection admission ("B3")** — per-axis special-casing of the
   predicate.
4. **Per-parameter Z cells in Selection ("B1-lite")** — breaks closed projection
   (`project(selection)` must return `sel.domain`).
5. **Kernel-dilated footprints** — kernel policy is not provider footprint; tolerance is request
   semantics (the vantage window *is* the consumer's tolerance).

## Validation

Stress-tested against a heterogeneous zoo: multi-level wind (resampler), cloud layers as **domain
declarations, not identities** (one functional, four cells; names are edge aliases), gusts/precip T
statistics (existing machinery), soil temperature (honestly excluded by a `[0,10]` window),
`pressure_msl`/isobaric (separate-frame requests per ADR-0002), station providers (X/Y *sample* axes —
the family generalizes horizontally). Much of the data-side design was **rediscovered** in ADR-0002's
existing text (fat cell absorbs offsets; vertical homogenization; coarsening follows the statistic);
the genuinely new half is the **matching** side, landed in ADR-0004.

## Docs updated with this session

Concern #24 moved out (→ ADR-0002/0004); concern #13 Z-half positioned; concern #20 wide-window note
(resampling, not selection); ADR-0002 request-mode bullet; ADR-0004 axis-kind matching + boundary;
glossary (vantage, maximal served cell); `parameters.md` vertical-carriage table; issues 002/003/005
aligned. **`cloud_cover` (total) added to v1** (decided this session): 6 canonical + 2 derived, the
agent product grows to 6 — a 1:1 `percent` channel on the same vendor fetch, and the first live
exercise of cell-statistic matching (v1-requirements / README / parameters.md updated).

## Open / continuation

- **Remaining 002 align questions**: internal-only carriage for `wind_u`/`wind_v` (no `requestable`
  flag exists); `Channel` output arity + `decode` signature; vendor unit strategy
  (request-and-verify vs convert); precipitation vendor-variable semantics.
- **002 implementation deltas**: mixed-shape request Domain (X/Y exact, T regular, Z continuous —
  retires the fake count-1 request Z axis); native Z declarations replace `_NEAR_SURFACE_Z`;
  axis-kind `matches` in `serves`; edge default vantage window constant; the `cloud_cover` scalar
  channel + `[0,TOA]` statistic-cell declaration.
- **Overlap Calculator** (low/mid/high → total) stays roadmap; the maximal-served-cell rule goes
  live at 002 with the total-only declaration.
