# 0019 — 2026-07-26 — Edge records: m5 minted, aligned, landed; both edges populated

## Work done

- **Edge record concept born and formalized** from a meta-discussion on artifact-supporting
  skills: artifacts split **normative** (validation obligation) vs **descriptive** (records);
  the wider taxonomy went to [m6](../../../tickets/01-0200-artifact-conventions-sweep.md) (Planned, own
  align precedes; carries the nested-numbering sketches). *Edge* was deliberately **not**
  generalized — glossary discipline keeps it the system's outer boundary.
- **Mid-session reframe (load-bearing):** the Edge record is the **architecture ↔ user-oriented
  design seam** — the living aggregation surface for one product edge (contract, invariants +
  validation state, edge-scoped concerns, staged roadmap), audience the project at work.
  Customer-facing text (tool descriptions, public API docs) **derives from it as a subset** —
  never the source. The first cut (customer-narrated contract doc) was wrong and is recorded
  struck-through in [m5](../../../tickets/done/01-0090-edge-records.md).
- **[m5](../../../tickets/done/01-0090-edge-records.md) landed (Done):** `/edge` skill,
  `EDGE-FORMAT.md` on the align skill's format shelf, Edge challenge rule in `/align`,
  Edge records in `/sync-arch`'s validated doc set, glossary **Edge record** term,
  `docs/edge/<surface>.md` home (unnumbered living docs), documentation-map row.
- **[MCP record](../../../edge/mcp.md) populated, `Normative`:** full `forecast_hourly` contract
  (absent-block ≠ `null` distinction, menu = exposure ∩ capability, edge-authored window and
  vantage), seven invariants each naming its validator, seven scoped concerns
  (#5 #10 #15 #29 #30 #36 #14), six roadmap stages (003c → 006 → 007 → 004 → 005 → 009).
- **Contradiction found and fixed:** `errors.py` claimed `RuntimeFailure` Arbiter fall-through;
  none exists (004's future). Docstring now states whole-request failure. Deterministic suite
  green (166 passed).
- **First `/sync-arch` pass over the record:** all seven validators exist and assert their
  promises; the vendor-null citation sharpened to `test_vendor_null_serializes_as_json_null`;
  tool self-description verified as a subset of the record.
- **[Embedding record](../../../edge/embedding.md) populated as deliberate `Stub`:** decided vs
  de-facto-not-promised vs open strata; two **⚠ unguarded** candidate invariants; concerns
  roster (#39 owner, #23, #12, #26, #35); five tentative roadmap stages (only mode-builders has
  real tickets).

## Open questions

- **m6 align** — registry home (extend the doc map vs sibling conventions doc), the sequencing
  scheme for nested subtickets (two sketch variants + maintenance-flag idea recorded in the
  ticket), renumber-vs-forward-only, and the full doc-roster classification. Depends on m5
  (done); ready when wanted.
- **Wire-level omission assertion** — the silent-omission invariant is validated at the Arbiter
  only; the MCP-payload assertion gap is noted in the record and owned by
  [009](../../../tickets/01-0190-error-taxonomy-partial-success.md).
- **Embedding graduation** — the record flips Normative when
  [#39](../../../concerns.md#39-python-embedding-surface-and-public-failures)'s facade/failure/
  compatibility decisions land; its Contract "Open" list is the drain gauge.
- **Alias desugaring** — architecture §Surface adapter describes desugaring aliases to
  functionals; code's mapping is identity today (product names are the functional ids). A seam
  statement, not drift — but a real alias layer is a contract change the `/align` Edge rule
  must catch.

## Continuation

- 003c / m4 landings now trip the `/align` Edge challenge rule against
  [docs/edge/mcp.md](../../../edge/mcp.md) (window semantics are Contract there); the landing edit
  moves Roadmap stage 1 into Contract.
- `/sync-arch` now mechanically audits Edge-record validators each pass.
- The m6 sweep should classify Edge records in the artifact registry and slim the skills that
  restate conventions.
